/**
 *@NApiVersion 2.x
 *@NScriptType Restlet
 */

// eslint-disable-next-line no-unused-vars
define(['N/record', 'N/error', 'N/workflow'], function (record, error, workflow) {
    // create cc token
    // update subscription record and activate
    // create invoice record and add donation lines
    // create customer payment record

    function post(context) {
      return null;
    }

    return {
      post: post,
    };
  });
